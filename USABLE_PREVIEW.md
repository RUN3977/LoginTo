# LoginTo 可用预览版

当前版本是本机可用预览版。数据只写入本机工程目录的 `.tmp`，包括 vault、runtime-state、device-identity、同步收据、备份包和采集日志；不需要公共网络登录。

三端预览遵循这几条核心原则：

- 桌面、手机、平板都有独立的本地 vault 和独立设备身份。
- 第一次连接必须面对面配对，通过二维码和 6 位校验码确认信任。
- 每次同步前都会显示对方设备、请求时间、连接方式、发送/接收/冲突摘要。
- 同步后会在三端显示同步收据和最近同步日志；失败记录会给出重新扫描和重新配对入口。
- 明文同步包会被接收端拒绝；同步包需要绑定可信设备身份。
- 如果确认后记录发生变化，会拒绝旧确认，要求重新预览。
- 冲突会进入可视化处理，可选择本机、对方、保留两份或手动合并。

## 启动

双击：

```cmd
LoginTo-Start.cmd
```

默认地址：

- 桌面端：http://127.0.0.1:4173
- 手机端：http://127.0.0.1:4177
- 平板端：http://127.0.0.1:4178

也可以用本机 App 窗口模式打开：

```cmd
LoginTo-App-Windows.cmd
```

只打开单端：

```cmd
LoginTo-App-Desktop.cmd
LoginTo-App-Phone.cmd
LoginTo-App-Tablet.cmd
```

## 检查

双击：

```cmd
LoginTo-Check.cmd
```

它会检查三端 `/api/status` 是否可达，并显示真实端口。也可以生成完整可用性报告：

```cmd
LoginTo-Report.cmd
```

报告路径：`.tmp\loginto-readiness-report.md`

## 停止与重置

停止三端预览：

```cmd
LoginTo-Stop.cmd
```

归档并重置演示数据：

```cmd
LoginTo-Reset-Demo.cmd
```

重置会先确认，再把当前 `.tmp` 归档到 `.tmp-archives`，不会直接删除旧预览数据。

## 验收与交付

运行完整验收：

```cmd
LoginTo-Acceptance.cmd
```

生成交付包：

```cmd
LoginTo-Package.cmd
```

交付目录：`dist\LoginTo-usable-preview`

## 当前可用能力

- 桌面端：记录增删改查、提醒中心、系统提醒桥接、本地加密备份与恢复验证、面对面配对、近场候选扫描、同步到手机/平板、冲突处理、同步日志、失败恢复入口。
- 手机端：记录增删改查、提醒中心、拍照采集、OCR 字段确认、加密附件入库、同步到桌面、同步日志、失败恢复入口、本地保险箱安全状态。
- 平板端：大屏整理记录、附件/备注复核、提醒处理、同步到桌面、冲突处理、同步日志、失败恢复入口、本地保险箱安全状态。
- 近场同步：支持可信设备检查、二维码配对、6 位校验码、同步预览确认、加密同步包、失败收据、重新扫描和重新配对。

## 当前限制

- 这仍是本机预览版，不是正式安装包。
- 传输通道仍以 localhost/局域网预览为主；蓝牙和热点直连目前是协议与 UI 就绪，不是正式系统级蓝牙传输。
- OCR 当前使用本地预览识别文本，已经支持字段确认和加密附件入库；正式手机相机和系统通知还需要后续原生适配。
