{
  "name": "@loginto/tablet",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "LoginTo tablet terminal app shell, sharing the mobile runtime with a large-screen review layout.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "preview": "node scripts/dev-server.mjs --open"
  }
}
