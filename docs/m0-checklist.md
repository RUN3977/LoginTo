# M0 Checklist

M0 的目标是完成正式写业务代码前的工程基线。

## 已完成

- [x] 产品规划文档。
- [x] 取消网页/PWA 交付方向。
- [x] 多终端工程骨架。
- [x] 根 README 和基础配置。
- [x] 终端架构说明。
- [x] 威胁模型初稿。
- [x] 核心包目录。
- [x] 记录模板 JSON Schema。
- [x] 保险库 manifest JSON Schema。
- [x] 本地契约验证脚本。

## 待进入 M1 前确认

- [ ] 是否初始化 Git 仓库。
- [ ] 桌面端是否确定使用 Tauri。
- [ ] 移动端是否确定使用 React Native / Expo Dev Client。
- [ ] 加密库是否确定以 libsodium 为第一选择。
- [ ] 移动端本地 OCR 具体方案：系统 OCR、ML Kit 或其他。
- [ ] 桌面端本地 OCR 具体方案：Tesseract 或系统 OCR。
- [ ] SQLite 适配层选型。
- [ ] 是否需要 Windows 优先，还是同时考虑 macOS。

## M1 入口条件

- 核心记录模板通过验证。
- 加密字段敏感级别策略已冻结第一版。
- 保险库 manifest 版本为 `1`。
- 同步变更日志和冲突策略已能表达基础场景。
- 不存在需要网页端才能完成的 MVP 功能。

## M1 建议任务

1. 实现 `vault-core` 内存模型。
2. 实现保险库 manifest 创建和校验。
3. 实现记录模板读取和默认字段生成。
4. 实现提醒规则的数据结构。
5. 实现附件元数据的数据结构。
6. 添加 `crypto-core` 的假加密测试适配器，仅用于单元测试。
7. 设计 SQLite adapter 的接口，不急于绑定具体库。
